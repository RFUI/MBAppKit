project 'MBAppKit.xcodeproj'

target 'Test-iOS' do
    platform :ios, '9.0'

    pod 'MBAppKit', :path => '.', :subspecs => [
        'ApplicationFont',
        'Button',
        'Input',
        'Worker'
    ]
end
